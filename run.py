#!/usr/bin/env python
from app import app
app.run(debug = True,host = '0.0.0.0', port = 5000) #added host, port stuff to see if it makes the program run

#user bin whatever may need to be changed... we'll see